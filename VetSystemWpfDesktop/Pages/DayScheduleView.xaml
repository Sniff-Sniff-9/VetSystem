<UserControl x:Class="VetSystemWpfDesktop.Pages.DayScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">

    <ListView ItemsSource="{Binding TimeSlots}" BorderThickness="0" HorizontalContentAlignment="Stretch">
        <!-- Hover строк -->
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Border x:Name="Bd" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8" CornerRadius="12">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListView.ItemContainerStyle>

        <!-- ItemTemplate -->
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <!-- Время -->
                        <ColumnDefinition Width="*"/>
                        <!-- Записи -->
                    </Grid.ColumnDefinitions>

                    <!-- Время слота -->
                    <TextBlock Text="{Binding TimeLabel}" 
                               FontSize="14" 
                               FontWeight="Medium" 
                               VerticalAlignment="Center" 
                               Foreground="{DynamicResource MaterialDesignBody}" />

                    <!-- Список записей -->
                    <ItemsControl Grid.Column="1" ItemsSource="{Binding Appointments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{Binding BackgroundBrush}" 
                                        CornerRadius="8" 
                                        Margin="0,4,0,0"
                                        Padding="8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayText}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                                        <TextBlock Text="{Binding TimeRange}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodySecondary}" Opacity="0.75"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>