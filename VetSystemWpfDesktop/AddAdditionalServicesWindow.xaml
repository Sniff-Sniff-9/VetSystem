<Window x:Class="VetSystemWpfDesktop.Pages.AddAdditionalServicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VetSystemWpfDesktop.Pages"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Добавить услугу"
        Height="420"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#E3F2FD">

    <Grid Margin="10">

        <materialDesign:Card Padding="20" UniformCornerRadius="12">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->

                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">

                    <materialDesign:PackIcon
                        Kind="Pill"
                        Width="28"
                        Height="28"
                        Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>

                    <TextBlock
                        Text="Добавить услугу"
                        FontSize="22"
                        FontWeight="SemiBold"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"/>

                </StackPanel>


                <!-- SEARCH -->

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="Search" Width="24" Height="24" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                    <TextBox Grid.Column="1" x:Name="SearchTextBox" Height="36"
             materialDesign:HintAssist.Hint="Поиск..."/>
                </Grid>


   

                <ListView Grid.Row="2"
                          x:Name="ServicesListView"
                          BorderThickness="0"
                          HorizontalContentAlignment="Stretch"
                          materialDesign:ElevationAssist.Elevation="Dp0" Margin="0,8,0,0">

                    <ListView.ItemContainerStyle>

                        <Style TargetType="ListViewItem">

                            <Setter Property="Padding" Value="0"/>

                            <Setter Property="Template">

                                <Setter.Value>

                                    <ControlTemplate TargetType="ListViewItem">

                                        <Border x:Name="RowBorder"
                                                BorderBrush="#E0E0E0"
                                                BorderThickness="0,0,0,1"
                                                Background="Transparent">

                                            <ContentPresenter/>

                                        </Border>

                                        <ControlTemplate.Triggers>

                                            <Trigger Property="IsMouseOver" Value="True">

                                                <Setter TargetName="RowBorder"
                                                        Property="Background"
                                                        Value="#F5F5F5"/>

                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">

                                                <Setter TargetName="RowBorder"
                                                        Property="Background"
                                                        Value="#E3F2FD"/>

                                                <Setter TargetName="RowBorder"
                                                        Property="BorderBrush"
                                                        Value="#03A9F4"/>

                                            </Trigger>

                                        </ControlTemplate.Triggers>

                                    </ControlTemplate>

                                </Setter.Value>

                            </Setter>

                        </Style>

                    </ListView.ItemContainerStyle>


         

                    <ListView.ItemTemplate>

                        <DataTemplate>

                            <Grid Margin="8">

                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>

                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal">

                                    <materialDesign:PackIcon
                                        Kind="Pill"
                                        Margin="0,0,6,0"
                                        Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>

                                    <TextBlock
                                        Text="{Binding ServiceName}"
                                        VerticalAlignment="Center"
                                        FontWeight="SemiBold"/>

                                </StackPanel>

                                <TextBlock
                                    Grid.Column="1"
                                    Text="{Binding Price, StringFormat='{}{0} ₽'}"
                                    VerticalAlignment="Center"/>

                            </Grid>

                        </DataTemplate>

                    </ListView.ItemTemplate>

                </ListView>


                <!-- BUTTONS -->

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,12,0,0">

                    <Button
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Content="Отмена"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"/>

                    <Button
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Content="Добавить"
                        Foreground="White"
                        Click="Add_Click"/>

                </StackPanel>

            </Grid>

        </materialDesign:Card>

    </Grid>

</Window>
