<Page x:Class="VetSystemWpfDesktop.Pages.PetsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" Background="#E3F2FD" Title="Питомцы">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- HEADER -->
        <StackPanel Orientation="Horizontal" Margin="8">
            <materialDesign:PackIcon Kind="Dog" Width="40" Height="40" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
            <TextBlock Text="Питомцы" FontSize="30" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
        </StackPanel>
        <!-- STATISTICS -->
        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <materialDesign:Card Margin="4" Padding="16" UniformCornerRadius="12">
                <StackPanel HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="Dog" Width="32" Height="32" HorizontalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                    <TextBlock Text="Всего питомцев" Margin="0,6" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="128" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="1" Margin="4" Padding="16" UniformCornerRadius="12">
                <StackPanel HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="CalendarPlus" Width="32" Height="32" Foreground="Green"  HorizontalAlignment="Center"/>
                    <TextBlock Text="Новые за месяц" Margin="0,6" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="12" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
        <!-- MAIN CARD -->
        <materialDesign:Card Grid.Row="2" Padding="18" UniformCornerRadius="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- TOOLBAR -->
                <DockPanel Margin="0,0,0,14">
                    <Button DockPanel.Dock="Right" Style="{StaticResource MaterialDesignRaisedButton}" Foreground="White" Height="36" Click="AddPetButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus"/>
                            <TextBlock Text="Добавить питомца" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Search" Width="24" Height="24" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                        <TextBox Width="200" Height="36" materialDesign:HintAssist.Hint="Поиск..."/>
                        <ComboBox Width="140" Height="36" materialDesign:HintAssist.Hint="Вид" Margin="10,0,0,0"/>
                        <ComboBox Width="140" Height="36" materialDesign:HintAssist.Hint="Пол" Margin="10,0,0,0"/>
                        <ComboBox Width="140" Height="36" materialDesign:HintAssist.Hint="Возраст" Margin="10,0,0,0"/>
                    </StackPanel>
                </DockPanel>
                <!-- TABLE -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- HEADER -->
                    <Grid Margin="8,0,8,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="220"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Имя" FontWeight="SemiBold"/>
                        <TextBlock Text="Вид" Grid.Column="1" FontWeight="SemiBold"/>
                        <TextBlock Text="Порода" Grid.Column="2" FontWeight="SemiBold"/>
                        <TextBlock Text="Дата рождения" Grid.Column="3" FontWeight="SemiBold"/>
                        <TextBlock Text="Пол" Grid.Column="4" FontWeight="SemiBold"/>
                        <TextBlock Text="Владелец" Grid.Column="5" FontWeight="SemiBold"/>
                        <TextBlock Text="Действия" Grid.Column="6" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    </Grid>
                    <!-- LIST -->
                    <ListView Grid.Row="1" x:Name="PetsListView" BorderThickness="0" HorizontalContentAlignment="Stretch" materialDesign:ElevationAssist.Elevation="Dp0">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border x:Name="Bd" Background="Transparent">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#F5F5F5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <!-- ROW TEMPLATE -->
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                                    <Grid Margin="8,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="220"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Dog" Margin="0,0,6,0" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding SpeciesName}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Breed}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding BirthDate, StringFormat=dd.MM.yyyy}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="4" Text="{Binding GenderName}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="5" Text="{Binding ClientName}" VerticalAlignment="Center"/>
                                        <!-- ACTIONS -->
                                        <StackPanel Grid.Column="6" Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Просмотреть" 
                                                    Name="CheckPetButton" Click="CheckPetButton_Click">
                                                <materialDesign:PackIcon Kind="EyeOutline"/>
                                            </Button>
                                            <Button Name="EditPetButton" Click="EditPetButton_Click">

                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">

                                                        <!-- по умолчанию запрещено -->
                                                        <Setter Property="IsEnabled" Value="False"/>

                                                        <Style.Triggers>
                                                            <!-- если питомец создан админом -->
                                                            <DataTrigger Binding="{Binding ClientUserId}" Value="{x:Null}">
                                                                <Setter Property="IsEnabled" Value="True"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>

                                                    </Style>
                                                </Button.Style>

                                                <materialDesign:PackIcon Kind="Pencil"/>

                                            </Button>
                                            <Button Name="DeletePetButton" Click="DeletePetButton_Click">

                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">

                                                        <Setter Property="IsEnabled" Value="False"/>

                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ClientUserId}" Value="{x:Null}">
                                                                <Setter Property="IsEnabled" Value="True"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>

                                                    </Style>

                                                </Button.Style>

                                                <materialDesign:PackIcon Kind="Delete"/>

                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="История">
                                                <materialDesign:PackIcon Kind="History"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>